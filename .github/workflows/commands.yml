on:
  workflow_dispatch:


jobs:
  example:
    runs-on: ubuntu-latest

    steps:
      - name: Set API Key
        run: echo "API_KEY=12345" 
      - name: Use API Key
        run: echo "Using API Key $API_KEY"
  
